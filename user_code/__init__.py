# Users can out all their custom code in the folder `user_code` to integrate the todo notifier in  manner they wish to
# This folder will never be used/added to git and so it would be safe for people to git pull on todo notifier to fetch
# latest code
